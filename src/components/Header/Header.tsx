import React from "react";

// components
import { Container, Title } from "./styles";

const Header = () => {
  return (
    <Container>
      <Title>Header</Title>
    </Container>
  );
};

export default Header;
